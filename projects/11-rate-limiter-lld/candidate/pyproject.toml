[project]
name = "ratelimiter"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.4"]
api = ["fastapi>=0.110", "pydantic>=2.5", "httpx>=0.26"]

[tool.pytest.ini_options]
testpaths = ["tests"]
